// queue using array - pop, prepend
// assuming 0 to be a free space in array

#include<stdio.h>


void prepend(int *arr, int value){
    
}

void get(int *arr){ //, int size
    printf("arr:", arr);
    printf("arr[0]:", arr[0]);
    int size = sizeof(arr)/sizeof(arr[0]);
    printf("size:", size);
    // return arr[]
}


int main(){
    int arr[10] = { 0 };
    // prepending values into array at the start
    // get values from array from the end
    get(arr);
    return 0;
}