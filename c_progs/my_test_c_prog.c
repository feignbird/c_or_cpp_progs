#include <stdio.h>
#include <stdlib.h>

/*
struct Person
{
	char name[64];
	int age;
};
*/


int main()
{
	/*
	int d=234;
	int *pd = &d;
	printf("hello world %x", &d);
	printf("\n\nThis is a pointer variable with value {%x} and value it is pointing to {%d}", pd, *pd);

	struct Person myNewPersonVar;
	printf("size of Person structure (in bytes): %d\n", sizeof(myNewPersonVar));

	char myCharArr[] = {'h', 'e', 'l', 'p'};
	printf("myCharArr: %c\n", myCharArr[0]);
	printf("(before) myNewPersonVar.name: %c\n", myNewPersonVar.name[0]);
	printf("(before) myNewPersonVar.age: %d\n", myNewPersonVar.age);
	myNewPersonVar.age = 21;
	myNewPersonVar.name[0] = 'A';
	printf("(after) myNewPersonVar.name: %c\n", myNewPersonVar.name[0]);
	printf("(after) myNewPersonVar.age: %i\n", myNewPersonVar.age);

	printf("(checking addr) &myNewPersonVar.name: %x\n", &myNewPersonVar.name);
	printf("(checking addr) &myNewPersonVar.age: %x\n", &myNewPersonVar.age);
	*/

	//long int myInt, k[5];
	char ch, A[]="Hello, world";
	printf("A: %s", A);
	return 0;
}